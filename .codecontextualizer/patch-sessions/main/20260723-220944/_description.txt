Intención: Crear SecureDemoPage con estado base (lista de items, mensaje, rol actual), método @Call addItem(String), formulario HTML funcional (input name="newItem" + botón) y lista renderizada con {{#each items as item}}.

Alcance: Se reemplaza únicamente el archivo jreactive-demo-spring/src/main/java/com/ciro/jreactive/SecureDemoPage.java. No se modifica HomePage, ni se añade navegación, ni @Authorize, ni login/logout. Es la base funcional mínima sobre la que se añadirá autorización en subintenciones siguientes.

Motivo: El archivo fue revertido 3 veces y existe en el repositorio actual pero no aparece en el contexto entregado. Se necesita una base estable y compilable que respete todas las convenciones de JReactive (AppPage, @Route, @Stateful, @State, @Call, template con text block) para que posteriores subintenciones puedan añadir @Authorize de forma quirúrgica.

Validación manual sugerida desde el punto de vista del usuario:
- Navegar directamente a http://localhost:8080/secure-demo.
- Confirmar que la página carga sin errores mostrando el rol "GUEST" y los 3 items semilla.
- Escribir texto en el input, presionar "Agregar" y ver el nuevo item al final de la lista con el mensaje de confirmación actualizado.
