Intención: Añadir a SecureDemoPage el método @Call login(String role) para switch de rol ADMIN/USER/GUEST que actualiza currentRole y message.
Alcance: Se modifica únicamente SecureDemoPage.java mediante un REPLACE_BLOCK quirúrgico que inserta el método login(String) entre el método addItem existente y el método template(). No se toca el template HTML, ni otros archivos, ni se añade @Authorize todavía (eso es subintención siguiente).
Motivo: La página ya tiene el campo currentRole="GUEST" y message listos para ser mutados. Faltaba el método @Call público que el frontend invocará para cambiar de rol en caliente, lo que prepara el terreno para la demo de @Authorize en deleteItem.
Validación manual sugerida desde el punto de vista del usuario:
- Navegar a /secure-demo y confirmar que el rol muestra "GUEST".
- Desde la consola del navegador ejecutar: fetch('/call/login', {method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({args:["ADMIN"]})}) y recargar la página.
- Confirmar que el rol ahora muestra "ADMIN" y el mensaje dice "Rol cambiado a ADMIN."
- Repetir con "USER" y "GUEST" para verificar que los tres roles funcionan y que un rol inválido cae a GUEST.
