Intención: Añadir estado base a SecureDemoPage con campos @State (items, message, currentRole) y poblar datos semilla en onInit, actualizando el template para mostrar rol actual y mensaje.

Alcance: Se modifica únicamente SecureDemoPage.java mediante 3 REPLACE_BLOCK quirúrgicos: (1) añadir imports List/ArrayList, (2) añadir campos @State + onInit entre la declaración de clase y el método template, (3) actualizar el contenido del template para renderizar {{currentRole}}, {{message}} y la lista {{#each items}}. No se reemplaza el archivo completo ni se toca otros archivos.

Motivo: Es la base funcional sobre la que se añadirá @Authorize en subintenciones siguientes. El contenido actual del archivo fue leído vía READ explícito en el turno previo y contiene únicamente un template stub sin lógica. Se usa REPLACE_BLOCK en lugar de REPLACE_FILE para minimizar el riesgo de pérdida funcional.

Validación manual sugerida desde el punto de vista del usuario:
- Navegar a /secure-demo.
- Ver el badge de rol "GUEST", el mensaje de bienvenida y los 3 items semilla renderizados en la lista.
- Confirmar que la página carga sin errores en consola.
