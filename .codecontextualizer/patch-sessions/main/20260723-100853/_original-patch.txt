<<<DESCRIPCION>>>
Intención: Corregir la aserción del test para validar la fuente de verdad (la memoria reactiva) en lugar del campo físico de la clase, el cual puede estar desactualizado si la tarea completó antes de que el componente tuviera su estado MOUNTED activado.

Alcance:
- DeferLifecycleTest.java: Modificar testDeferSuccess para obtener el valor mediante `page.getRawBindings().get("fastData").get()`.

Motivo: Cuando `executeDeferred` ejecuta `rv.set()`, dispara el listener en `HtmlComponent` que actualiza el campo físico de la clase. Sin embargo, este listener respeta un `ActiveGuard` que evita escrituras si el estado no es `MOUNTED`. En un entorno real, el renderizado (que pautea el mount) ocurre antes de que el `@Defer` (latencia de red/BD) termine, pero en un test unitario el `CompletableFuture` puede resolver instantáneamente, provocando un "race condition" en el test. La fuente de verdad real del framework siempre es el `ReactiveVar`.

Validación manual sugerida:
- Ejecutar el módulo de pruebas `jreactive-runtime-jvm` y verificar que la prueba `DeferLifecycleTest` finalice verde.
<<<END_DESCRIPCION>>>

<<<FILE: jreactive-runtime-jvm/src/test/java/com/ciro/jreactive/DeferLifecycleTest.java>>>
<<<OP: REPLACE_BLOCK>>>
<<<FIND>>>
    @Test
    @DisplayName("Éxito: la tarea diferida carga datos correctamente")
    void testDeferSuccess() throws Exception {
        DeferPage page = new DeferPage();
        page._initIfNeeded();
        page._mountRecursive();

        Thread.sleep(500);

        assertThat(page.fastData).containsExactly("A", "B");
    }
<<<REPLACE>>>
    @Test
    @DisplayName("Éxito: la tarea diferida carga datos correctamente")
    void testDeferSuccess() throws Exception {
        DeferPage page = new DeferPage();
        page._initIfNeeded();
        page._mountRecursive();

        Thread.sleep(500);

        // Validamos la memoria reactiva (fuente de verdad) en lugar del campo físico 
        // para evitar condiciones de carrera si el hilo del test completa antes que el activeGuard del componente se habilite.
        Object fastData = page.getRawBindings().get("fastData").get();
        assertThat(fastData).isEqualTo(List.of("A", "B"));
    }
<<<END>>>