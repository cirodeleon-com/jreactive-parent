<<<DESCRIPCION>>>
Intención:
Eliminar la interpolación ${...} del template de SecureDemoPage reemplazándola por concatenación Java real, para que los valores de currentRole y message se rendericen correctamente sin depender de características del motor de plantillas no verificadas.

Alcance:
- Modifica únicamente 2 líneas dentro del método template() de SecureDemoPage.java mediante REPLACE_BLOCK quirúrgico.
- No reemplaza el archivo completo. No tocaimports, onInit, addItem ni ninguna otra lógica.
- No aborda el error de JaCoCo (preexistente en jreactive-apt).

Motivo:
En Java text blocks, ${...} es texto literal (no se interpola). Si el motor de plantillas de JReactive no lo procesa, la página mostraría literalmente "${currentRole}" en lugar del valor real. La concatenación Java garantiza el render correcto.

Validación manual sugerida desde el punto de vista del usuario:
- Navegar a /secure-demo y confirmar que se ve "guest" (no "${currentRole}") y el mensaje real (no "${message}").
<<<END_DESCRIPCION>>>

<<<FILE: jreactive-demo-spring/src/main/java/com/ciro/jreactive/SecureDemoPage.java>>>
<<<OP: REPLACE_BLOCK>>>
<<<FIND>>>
                  <p>Rol actual: <span class="role-tag">${currentRole}</span></p>
                  <p class="state-message">${message}</p>
<<<REPLACE>>>
                  <p>Rol actual: <span class="role-tag">""" + escapeHtml(this.currentRole) + """</span></p>
                  <p class="state-message">""" + escapeHtml(this.message) + """</p>
<<<END>>>