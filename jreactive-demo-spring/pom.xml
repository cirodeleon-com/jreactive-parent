<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- hereda la configuración y el dependencyManagement del padre -->
  <parent>
    <groupId>com.ciro</groupId>
    <artifactId>jreactive-parent</artifactId>
    <version>0.0.1-SNAPSHOT</version>
  </parent>

  <artifactId>jreactive-demo-spring</artifactId>
  <name>jreactive demo spring</name>
  <packaging>jar</packaging>

  <!-- 🔑 propiedad que faltaba -->
  <properties>
    <!-- todos los sub‑artefactos jreactive usan esta propiedad internamente;
         la igualamos a la versión del proyecto para que “resuelva” -->
    <jreactive.version>${project.version}</jreactive.version>

    <!-- si tu parent ya define java.version y spring‑boot.version,
         estas líneas pueden omitirse -->
    <java.version>17</java.version>
  </properties>

  <dependencies>
    <!-- único starter necesario en tiempo de ejecución -->
    <dependency>
      <groupId>com.ciro</groupId>
      <artifactId>jreactive-starter-spring</artifactId>
      <version>${project.version}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>

      <!-- versión fijada en <pluginManagement> del parent -->
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>

      <!-- compilación + annotation processor -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <!-- usa la versión que definiste en el parent (3.8.1) -->
        <configuration>
          <source>${java.version}</source>
          <target>${java.version}</target>

          <annotationProcessorPaths>
            <path>
              <groupId>com.ciro</groupId>
              <artifactId>jreactive-apt</artifactId>
              <version>${project.version}</version>
            </path>
          </annotationProcessorPaths>

          <!-- necesarios para que el processor acceda al API del compilador -->
          <fork>true</fork>
          <compilerArgs>
            <arg>--add-exports</arg>
            <arg>jdk.compiler/com.sun.source.util=ALL-UNNAMED</arg>
            <arg>--add-exports</arg>
            <arg>jdk.compiler/com.sun.source.tree=ALL-UNNAMED</arg>
            <arg>-parameters</arg>
          </compilerArgs>
        </configuration>
      </plugin>

    </plugins>
  </build>
</project>
